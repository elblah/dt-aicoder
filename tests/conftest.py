"""
Test configuration file.
"""

import sys
import os

# Add the aicoder package to the Python path
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

# Configuration for unittest tests can be added here if needed
